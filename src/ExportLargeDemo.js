const WelcomeDemo = ()=>{ //functional component that returns a React element (JSX) |arrow function
  return(
    <h1>Welcome to react Js : Exporting primary component</h1>
  );
}

export default WelcomeDemo; //Exporting primary component